import React from 'react';

const Mycomponents = (props) => (
    <div>
        <h1>..............................</h1>
        {props.message}
        {props.name}
</div>
)
export default Mycomponents